# mybudgeter

Todo:
- Show spending without savings. Savings is not really an expense. 
- Make savings a special category. If we have a category called "Savings" then it should have a "Add Savings" button instead of "Add Expense". Maybe add a way to specify if the category is a type of savings or expense category too.
- "Monthly Spending" text should be "Monthly Savings" for savings category"
- Remaining amount in summary should still subtract how much we put into savings. Remaining should just be how much extra we can put to expenses.
- Expense categories should show a green progress bar when progress is low. orange when the progress bar is close to full. and red when it is near full or overly full. 
- Savings category should be the opposite. Red when low, orange when close to full, green when full. The goal of the savings category is to be near or above the target.
- Complete summary should show how much we are saving in each period.
- Make "Add Expense" or "Add Savings" button be a better color while still having contrast
- Small categories like 20% and 10% look visually the same. Figure out a way to make it more visual. 
- I updated "Target" to "Monthly Target" and "Spending" to "Monthly Spending" manually. Please go through the code and check if we should update any variables or names to make the code be consistent to the field names.
- Add note on how save file is stored locally in browser. Downloading should be down for safe backup. Then import save file overwrites current data.
- Add "are you sure?" warning before importing and overwriting save file.
- Add drag and drop rearrange in each category.
- Add sort by cost, name, or period in each category
- Add edit feature to each expense or saving item
- add button to duplicate expense
- add ability to add expense multiplier. For example a $35 expense times the quantity or number of times I need to buy it.
- make income entry editable and add color to the entry to make it stand out just a little