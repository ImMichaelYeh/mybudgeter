function renderApp(state, summary, categories, allExpenses, saveMessage = '') {
  const money = (v) => `$${Number(v || 0).toFixed(2)}`;
  const esc = (v) => String(v ?? '').replace(/[&<>"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
  const frequencies = window.budget.FREQUENCY_OPTIONS;
  const options = (selected) => frequencies.map((x) => `<option value="${x.value}" ${x.value === selected ? 'selected' : ''}>${x.label}</option>`).join('');
  const periods = [['weekly', 'Weekly'], ['biweekly', 'Biweekly'], ['monthly', 'Monthly'], ['quarterly', 'Quarterly'], ['annual', 'Annual']];
  const incomeEditor = (income) => `<form class="income-edit-form" data-edit-income-form="${income.id}" ${state.app.editingIncomeId === income.id ? '' : 'hidden'}><div class="field-row"><input name="name" value="${esc(income.name)}" required /><input name="amount" type="number" min="0" step="0.01" value="${income.amount}" required /><select name="frequency">${options(income.frequency)}</select></div><textarea name="notes">${esc(income.notes || '')}</textarea><div class="actions"><button type="submit">Save</button><button type="button" class="secondary" data-cancel-edit-income>Cancel</button></div></form>`;
  const categoryHeader = (category) => state.app.editingCategoryId === category.id ? `<form class="category-edit-form" data-edit-category-form="${category.id}"><div class="field-row"><input name="name" value="${esc(category.name)}" required /><input name="percentage" type="number" min="0" step="0.1" value="${category.percentage}" required /></div><label class="check"><input name="isSavings" type="checkbox" ${category.isSavings ? 'checked' : ''} /> Savings Category</label><div class="actions"><button type="submit">Save</button><button type="button" class="secondary" data-cancel-edit-category>Cancel</button></div></form>` : `<div class="column-header"><div><h3>${esc(category.name)}</h3><p>${category.percentage}% · ${category.isSavings ? 'Savings' : 'Expenses'}</p></div><div class="actions"><button class="ghost small-button" data-edit-category="${category.id}">Edit</button><button class="ghost small-button" data-remove-category="${category.id}">Remove</button></div></div>`;
  const addItem = (category) => `<form class="expense-form" data-category-id="${category.id}"><input name="name" placeholder="${category.isSavings ? 'Savings name' : 'Expense name'}" required /><div class="field-row"><input name="amount" type="number" min="0" step="0.01" placeholder="Amount" required /><input name="multiplier" type="number" min="1" step="1" value="1" placeholder="Qty" /><select name="frequency">${options('monthly')}</select></div><input name="group" placeholder="Group (optional)" /><div class="actions"><button type="submit">Save</button><button type="button" class="secondary" data-cancel-expense>Cancel</button></div></form>`;
  const total = window.budget.getCategoryPercentageTotal(state);
  const incomeRows = state.incomes.map((income) => `<div class="chip income-item"><div><strong>${esc(income.name)}</strong><div class="small">${money(income.amount)} · ${esc(income.frequency)}</div>${income.notes ? `<div class="income-note">Note: ${esc(income.notes)}</div>` : ''}</div><div class="actions"><button class="ghost small-button" data-edit-income="${income.id}">Edit</button><button class="ghost small-button" data-remove-income="${income.id}">Remove</button></div></div>${incomeEditor(income)}`).join('');
  const board = categories.map((category, index) => {
    const detail = summary.categories.find((x) => x.id === category.id); const usage = detail?.usagePercent || 0;
    const color = category.isSavings ? (usage >= 90 ? 'good' : usage >= 60 ? 'warn' : 'bad') : (usage >= 90 ? 'bad' : usage >= 60 ? 'warn' : 'good');
    const expenseSort = state.app.expenseSorts?.[category.id] || state.app.expenseSort || 'manual';
    const items = window.budget.sortExpenses(allExpenses.filter((x) => x.categoryId === category.id), expenseSort);
    const list = items.map((item) => state.app.editingExpenseId === item.id ? `<form class="expense-edit-form" data-edit-expense-form="${item.id}"><input name="name" value="${esc(item.name)}" required /><div class="field-row"><input name="amount" type="number" min="0" step="0.01" value="${item.amount}" required /><input name="multiplier" type="number" min="1" step="1" value="${item.multiplier || 1}" required /><select name="frequency">${options(item.frequency)}</select></div><input name="group" value="${esc(item.group)}" placeholder="Group (optional)" /><div class="actions"><button type="submit">Save</button><button type="button" class="secondary" data-cancel-edit-expense>Cancel</button></div></form>` : `<div class="expense-item" draggable="true" data-expense-id="${item.id}"><div><strong>${esc(item.name)}</strong><div class="small">${money(item.amount * (item.multiplier || 1))} · ${esc(item.frequency)}${item.multiplier > 1 ? ` · ×${item.multiplier}` : ''}</div>${item.group ? `<div class="tag">${esc(item.group)}</div>` : ''}</div><div class="actions"><button class="ghost small-button" data-edit-expense="${item.id}">Edit</button><button class="ghost small-button" data-duplicate-expense="${item.id}">Copy</button><button class="ghost small-button" data-remove-expense="${item.id}">×</button></div></div>`).join('');
    return `<article class="category-column" draggable="true" data-category-id="${category.id}" style="--accent:${['#2563eb','#7c3aed','#059669','#ea580c','#dc2626','#0891b2'][index % 6]};--column-percent:${Math.max(10, category.percentage)}"><div class="column-header"><div><h3>${esc(category.name)}</h3><p>${category.percentage}% · ${category.isSavings ? 'Savings' : 'Expenses'}</p></div><div class="actions"><button class="ghost small-button" data-edit-category="${category.id}">Edit</button><button class="ghost small-button" data-remove-category="${category.id}">Remove</button></div></div><div class="column-metrics"><div><span>Monthly Target</span><strong>${money(detail?.monthlyTarget)}</strong></div><div><span>${category.isSavings ? 'Monthly Savings' : 'Monthly Spending'}</span><strong>${money(detail?.monthlySpent)}</strong></div><div><span>Remaining</span><strong>${money(detail?.monthlyRemaining)}</strong></div></div><div class="progress-bar ${color}"><span style="width:${Math.min(100, usage)}%"></span></div>${state.app.addingExpenseCategoryId === category.id ? addItem(category) : `<button type="button" class="expense-add-button" data-show-expense-form="${category.id}">${category.isSavings ? 'Add savings' : 'Add expense'}</button>`}<div class="expense-toolbar"><label>Sort<select data-expense-sort="${category.id}">${['manual', 'amount', 'name', 'frequency', 'group'].map((x) => `<option value="${x}" ${expenseSort === x ? 'selected' : ''}>${x[0].toUpperCase() + x.slice(1)}</option>`).join('')}</select></label></div><div class="expense-list">${list || '<div class="empty-item">No items yet.</div>'}</div></article>`;
  }).join('');
  const summaryCards = periods.map(([key, label]) => { const x = summary.periods[key]; return `<div class="summary-card"><h3>${label}</h3><div class="summary-row"><span>Income</span><strong>${money(x.income)}</strong></div><div class="summary-row"><span>Spending</span><strong>${money(x.expenses)}</strong></div><div class="summary-row"><span>Savings</span><strong>${money(x.savings)}</strong></div><div class="summary-row"><span>Uncategorized</span><strong>${money(x.remaining)}</strong></div><div class="summary-row total-unspent"><span>Total Unspent</span><strong>${money(x.savings + x.remaining)}</strong></div></div>`; }).join('');
  return `<header><div><h1>mybudgeter</h1><p class="small">A simple budgeting board.</p></div><div class="actions"><span class="save-status" data-save-status>${esc(saveMessage)}</span><button type="button" data-download-save>Download save file</button><label class="button-like secondary">Import save file<input data-import-save type="file" accept="application/json,.json" /></label></div></header><section class="card"><div class="section-heading"><div><h2>Net Income</h2></div></div><div class="collapsible-card"><div class="income-summary"><div class="summary-inline"><span>${state.incomes.length || 'No'} income source${state.incomes.length === 1 ? '' : 's'} · ${money(summary.monthlyIncome)} / month</span><button type="button" class="link-button hint" data-toggle-income>${state.app.isIncomeOpen === false ? 'click to expand' : 'click to collapse'}</button></div></div>${state.app.isIncomeOpen === false ? '' : `<div class="collapsible-body"><form id="income-form"><div class="field-row"><input name="name" placeholder="Name" required /><input name="amount" type="number" min="0" step="0.01" placeholder="Amount" required /><select name="frequency">${options('biweekly')}</select></div><textarea name="notes" placeholder="Notes"></textarea><button type="submit">Add income</button></form><div class="pill-list">${incomeRows || '<div class="empty-state">No income yet.</div>'}</div></div>`}</div></section><section class="card"><div class="section-heading"><div><h2>Budget board</h2></div>${state.app.isAddingCategory ? `<form id="category-form" class="inline-form"><input name="name" placeholder="Category name" required /><input name="percentage" type="number" min="0" step="0.1" placeholder="Percent" required /><label class="check"><input name="isSavings" type="checkbox" /> Savings</label><button type="submit">Add category</button><button type="button" class="secondary" data-cancel-category>Cancel</button></form>` : '<button type="button" data-show-category-form>Add category</button>'}</div><div class="category-form-meta"><span>Used: ${total.toFixed(1)}%</span><span>${total > 100 ? `Over: ${(total - 100).toFixed(1)}%` : `Remaining: ${(100 - total).toFixed(1)}%`}</span></div><div class="board-controls"><label>Category order<select data-category-sort><option value="manual">Custom</option><option value="percentage" ${state.app.categorySort === 'percentage' ? 'selected' : ''}>Percentage</option><option value="name" ${state.app.categorySort === 'name' ? 'selected' : ''}>Name</option></select></label></div><div class="board">${board || '<div class="empty-state">No categories yet.</div>'}</div></section><section class="card"><div class="section-heading"><div><h2>Complete summary</h2><p class="small">Remaining is available after spending and savings.</p></div></div><div class="summary-grid-wide">${summaryCards}</div></section>`;
}
window.renderApp = renderApp;
